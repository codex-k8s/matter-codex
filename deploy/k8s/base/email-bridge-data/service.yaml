{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "email-bridge-postgresql",
    "namespace": "kodex-system",
    "labels": {
      "app.kubernetes.io/name": "email-bridge-postgresql"
    }
  },
  "spec": {
    "selector": {
      "app.kubernetes.io/name": "email-bridge-postgresql"
    },
    "ports": [
      {
        "name": "postgresql",
        "port": 5432,
        "targetPort": 5432
      }
    ]
  }
}
