apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kodex-system
resources:
  - serviceaccount.yaml
  - credential-rbac.yaml
  - email-projection.yaml
  - deployment.yaml
  - service.yaml
  - poddisruptionbudget.yaml
  - migration-job.yaml
  - broker-bootstrap-job.yaml
  - networkpolicy.yaml
  - servicemonitor.yaml
  - prometheusrule.yaml
  - dashboard.yaml
components:
  - ../../components/internal-rpc-authority-control-plane-verifier
  - ../../components/internal-rpc-authority-control-plane-issuer
configMapGenerator:
  - name: control-plane-runtime
    literals:
      - CONTROL_PLANE_NATS_STREAM=CONTROL_PLANE
      # 32 GiB account budget minus the mandatory 16 MiB session-revocation stream.
      - CONTROL_PLANE_NATS_MAX_BYTES=34342961152
generatorOptions:
  disableNameSuffixHash: true
patches: null
